export interface DishStep {
    title: string;
    description?: string;
    order: number;
}

export interface Step {
    id: number;
    title: string;
    description?: string;
    order: number; 
}